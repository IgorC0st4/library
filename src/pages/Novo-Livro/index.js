import * as React from 'react';
import { SafeAreaView } from 'react-native';
import { Text } from 'react-native-elements';

export default function Home() {


    return (
        <SafeAreaView>
            <Text h1>Hello Novo Livro</Text>
        </SafeAreaView>
    );
}